// App drawer — simplified placeholder.
//
// The sidebar is now fully handled by RoleShell (role_shell.dart), which
// renders a 260px slide-in sidebar with brand header, nav groups, and a
// bottom user card with sign-out. This file is kept only as a re-export
// point so that any legacy imports still compile. No separate drawer
// widget is needed.

export 'role_shell.dart';
