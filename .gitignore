# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log

# Secrets & credentials — NEVER commit
.gh-token
*.keystore
*.jks
key.properties

# Local databases
db/*.db
db/*.db-journal

# Tool results & scratch dirs (contain scraped data, not source)
/tool-results/
/upload/
/preview-*.png

# Internal agent handover docs (not part of the product source)
/agent-ctx/
/worklog.md

# Sandbox dev infra (local only)
/.zscripts/

# Research / scraped reference data (never deploy this — may contain third-party keys)
/research/

# Skills directory
/skills/
# Token files — never commit
.gh-token
.gh-token-2
gh-token.txt

# GitHub Actions workflow (push with a token that has workflow scope)
.github/workflows/
