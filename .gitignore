# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
test
prompt

server.log

# Secrets & credentials — NEVER commit
.gh-token
*.keystore
*.jks
key.properties
# Firebase service account JSON (contains private key — read from env var in prod)
firebase-adminsdk-*.json
*-firebase-adminsdk-*.json
# google-services.json IS committed — it's needed for the Android build and
# contains only the public app ID + API key (no secrets).

# Local databases
db/*.db
db/*.db-journal

# Tool results & scratch dirs (contain scraped data, not source)
/tool-results/
/upload/
/preview-*.png

# Internal agent handover docs (not part of the product source)
/agent-ctx/
/worklog.md

# Sandbox dev infra (local only)
/.zscripts/

# Research / scraped reference data (never deploy this — may contain third-party keys)
/research/

# Skills directory
/skills/
# Token files — never commit
.gh-token
.gh-token-2
gh-token.txt

# GitHub Actions workflow — committed so tag pushes trigger the APK build.
# (Requires a PAT with `workflow` scope to push changes to this path.)
build_log.txt
*.apk
concordia-college.apk
